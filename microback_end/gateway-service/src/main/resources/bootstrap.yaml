spring:
  application:
    name: gateway-service
  cloud:
    config:
      enable: true
      uri: http://localhost:8081
      username: admin
      password: admin

  boot:
    admin:
      client:
        url: http://localhost:8086